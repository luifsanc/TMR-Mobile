<ContentPage xmlns:shared="clr-namespace:tmr_mobile.Views.Shared" xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:tmr_mobile.ViewModels"
             x:Class="tmr_mobile.Views.Reportes.ReportesPage"
             x:DataType="vm:ReportesViewModel"
             Title="Reportes">
    <ContentPage.ToolbarItems>
        <ToolbarItem IconImageSource="direccion.png" Text="Inicio" Clicked="OnHomeClicked" />
    </ContentPage.ToolbarItems>
    <Grid RowDefinitions="*,80">
        <ContentView Grid.Row="0">

    <VerticalStackLayout Padding="20">
        <Label Text="Pantalla de Reportes"
               FontSize="24"
               HorizontalOptions="Center" />
    </VerticalStackLayout>

        </ContentView>
        <shared:BottomNavBar Grid.Row="1" />
    </Grid>
</ContentPage>
