<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns:shared="clr-namespace:tmr_mobile.Views.Shared"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:vm="clr-namespace:tmr_mobile.ViewModels"
    xmlns:model="clr-namespace:tmr_mobile.Models.Configuracion"
    x:Class="tmr_mobile.Views.Configuracion.CatalogosPage"
    x:DataType="vm:CatalogosConfigViewModel"
    Title="Catálogos"
    BackgroundColor="{AppThemeBinding Light={StaticResource BackgroundLight}, Dark={StaticResource DarkBackgroundLight}}">
    <Grid RowDefinitions="*,36">
        <ContentView Grid.Row="0">
            <Grid
                RowDefinitions="Auto,Auto,Auto,Auto,*"
                Padding="12"
                RowSpacing="6">

                <Grid Grid.Row="0" VerticalOptions="Center">
                    <Label Text="Catálogos"
                           VerticalOptions="Center"
                           Style="{StaticResource CatalogPageTitle}"
                           FontSize="22" />
                </Grid>

                <Grid Grid.Row="1" ColumnDefinitions="*,*,*" ColumnSpacing="8">
                    <Border Grid.Column="0" HeightRequest="56" Padding="5" Style="{StaticResource CatalogMetricCard}">
                        <VerticalStackLayout Spacing="1" VerticalOptions="Center">
                            <HorizontalStackLayout Spacing="4" HorizontalOptions="Center">
                                <Border WidthRequest="24" HeightRequest="24" Padding="5"
                                        BackgroundColor="{AppThemeBinding Light={StaticResource ErrorSurface}, Dark={StaticResource DarkErrorSurface}}"
                                        StrokeThickness="0" StrokeShape="RoundRectangle 8">
                                    <Image Source="catalog_total_items.png" WidthRequest="14" HeightRequest="14" Aspect="AspectFit" />
                                </Border>
                                <Label Text="{Binding TotalItems}" FontFamily="InterSemiBold" FontSize="17" FontAttributes="None"
                                       TextColor="{AppThemeBinding Light={StaticResource Primary}, Dark={StaticResource DarkPrimary}}" VerticalTextAlignment="Center" />
                            </HorizontalStackLayout>
                            <Label Text="Total de ítems" FontFamily="InterRegular" FontSize="9"
                                   TextColor="{AppThemeBinding Light={StaticResource TextSecondary}, Dark={StaticResource DarkTextSecondary}}" HorizontalTextAlignment="Center" />
                        </VerticalStackLayout>
                    </Border>
                    <Border Grid.Column="1" HeightRequest="56" Padding="5" Style="{StaticResource CatalogMetricCard}">
                        <VerticalStackLayout Spacing="1" VerticalOptions="Center">
                            <HorizontalStackLayout Spacing="4" HorizontalOptions="Center">
                                <Border WidthRequest="24" HeightRequest="24" Padding="5"
                                        BackgroundColor="{AppThemeBinding Light={StaticResource SuccessSurface}, Dark={StaticResource DarkSuccessSurface}}"
                                        StrokeThickness="0" StrokeShape="RoundRectangle 8">
                                    <Image Source="catalog_active.png" WidthRequest="14" HeightRequest="14" Aspect="AspectFit" />
                                </Border>
                                <Label Text="{Binding TotalActivos}" FontFamily="InterSemiBold" FontSize="17" FontAttributes="None"
                                       TextColor="{AppThemeBinding Light={StaticResource Primary}, Dark={StaticResource DarkPrimary}}" VerticalTextAlignment="Center" />
                            </HorizontalStackLayout>
                            <Label Text="Activos" FontFamily="InterRegular" FontSize="9"
                                   TextColor="{StaticResource Accent}" HorizontalTextAlignment="Center" />
                        </VerticalStackLayout>
                    </Border>
                    <Border Grid.Column="2" HeightRequest="56" Padding="5" Style="{StaticResource CatalogMetricCard}">
                        <VerticalStackLayout Spacing="1" VerticalOptions="Center">
                            <HorizontalStackLayout Spacing="4" HorizontalOptions="Center">
                                <Border WidthRequest="24" HeightRequest="24" Padding="5"
                                        BackgroundColor="{AppThemeBinding Light={StaticResource Secondary}, Dark={StaticResource DarkSecondary}}"
                                        StrokeThickness="0" StrokeShape="RoundRectangle 8">
                                    <Image Source="catalog_inactive.png" WidthRequest="14" HeightRequest="14" Aspect="AspectFit" />
                                </Border>
                                <Label Text="{Binding TotalInactivos}" FontFamily="InterSemiBold" FontSize="17" FontAttributes="None"
                                       TextColor="{AppThemeBinding Light={StaticResource Primary}, Dark={StaticResource DarkPrimary}}" VerticalTextAlignment="Center" />
                            </HorizontalStackLayout>
                            <Label Text="Inactivos" FontFamily="InterRegular" FontSize="9"
                                   TextColor="{StaticResource Error}" HorizontalTextAlignment="Center" />
                        </VerticalStackLayout>
                    </Border>
                </Grid>

                <Grid Grid.Row="2" ColumnDefinitions="*,Auto,Auto" ColumnSpacing="5">
                    <Border Grid.Column="0" HeightRequest="32" Style="{StaticResource CatalogInputBorder}" Padding="6,0">
                        <Grid ColumnDefinitions="Auto,*" ColumnSpacing="5">
                            <Label Grid.Column="0" Text="⌕" FontFamily="InterRegular" FontSize="15"
                                   TextColor="{AppThemeBinding Light={StaticResource TextLight}, Dark={StaticResource DarkTextLight}}" VerticalTextAlignment="Center" />
                            <Entry Grid.Column="1"
                                   Text="{Binding Busqueda}"
                                   Placeholder="Buscar..."
                                   HeightRequest="30"
                                   MinimumHeightRequest="30"
                                   FontSize="10"
                                   Style="{StaticResource CatalogInput}" />
                        </Grid>
                    </Border>

                    <Button Grid.Column="1"
                            Text="Filtro ⌄"
                            Command="{Binding AlternarFiltrosCommand}"
                            WidthRequest="78"
                            HeightRequest="32"
                            MinimumHeightRequest="32"
                            Padding="6,0"
                            FontFamily="InterSemiBold"
                            FontSize="9"
                            BackgroundColor="{AppThemeBinding Light={StaticResource SurfaceColor}, Dark={StaticResource DarkSurfaceColor}}"
                            TextColor="{AppThemeBinding Light={StaticResource Primary}, Dark={StaticResource DarkPrimary}}"
                            BorderColor="{AppThemeBinding Light={StaticResource BorderColor}, Dark={StaticResource DarkBorderColor}}"
                            BorderWidth="1"
                            CornerRadius="9" />

                    <Button Grid.Column="2" Text="+ Nuevo" Command="{Binding NuevoCommand}"
                            WidthRequest="64" HeightRequest="32" MinimumHeightRequest="32"
                            Padding="3,0" FontSize="10"
                            Style="{StaticResource PrimaryButton}" />
                </Grid>

                <Border Grid.Row="3"
                        IsVisible="{Binding MostrarFiltros}"
                        Padding="8"
                        BackgroundColor="{AppThemeBinding Light={StaticResource SurfaceColor}, Dark={StaticResource DarkSurfaceColor}}"
                        Stroke="{AppThemeBinding Light={StaticResource BorderColor}, Dark={StaticResource DarkBorderColor}}"
                        StrokeThickness="1"
                        StrokeShape="RoundRectangle 10">
                    <VerticalStackLayout Spacing="8">
                        <!-- Primera fila: filtro por estado -->
                        <HorizontalStackLayout Spacing="8">
                            <Border Padding="12,6"
                                    BackgroundColor="{AppThemeBinding Light={StaticResource SurfaceColor}, Dark={StaticResource DarkSurfaceColor}}"
                                    Stroke="{AppThemeBinding Light={StaticResource BorderColor}, Dark={StaticResource DarkBorderColor}}"
                                    StrokeThickness="1"
                                    StrokeShape="RoundRectangle 14">
                                <Border.Triggers>
                                    <DataTrigger TargetType="Border" Binding="{Binding FiltroEstado}" Value="Todos">
                                        <Setter Property="BackgroundColor" Value="{AppThemeBinding Light={StaticResource Primary}, Dark={StaticResource DarkPrimary}}" />
                                        <Setter Property="Stroke" Value="{AppThemeBinding Light={StaticResource Primary}, Dark={StaticResource DarkPrimary}}" />
                                    </DataTrigger>
                                </Border.Triggers>
                                <Border.GestureRecognizers>
                                    <TapGestureRecognizer Command="{Binding MostrarTodosCommand}" />
                                </Border.GestureRecognizers>
                                <Label Text="Todos"
                                       FontFamily="InterSemiBold"
                                       FontSize="11"
                                       TextColor="{AppThemeBinding Light={StaticResource Primary}, Dark={StaticResource DarkPrimary}}">
                                    <Label.Triggers>
                                        <DataTrigger TargetType="Label" Binding="{Binding FiltroEstado}" Value="Todos">
                                            <Setter Property="TextColor" Value="{StaticResource White}" />
                                        </DataTrigger>
                                    </Label.Triggers>
                                </Label>
                            </Border>

                            <Border Padding="12,6"
                                    BackgroundColor="{AppThemeBinding Light={StaticResource SurfaceColor}, Dark={StaticResource DarkSurfaceColor}}"
                                    Stroke="{AppThemeBinding Light={StaticResource BorderColor}, Dark={StaticResource DarkBorderColor}}"
                                    StrokeThickness="1"
                                    StrokeShape="RoundRectangle 14">
                                <Border.Triggers>
                                    <DataTrigger TargetType="Border" Binding="{Binding FiltroEstado}" Value="Activos">
                                        <Setter Property="BackgroundColor" Value="{AppThemeBinding Light={StaticResource Primary}, Dark={StaticResource DarkPrimary}}" />
                                        <Setter Property="Stroke" Value="{AppThemeBinding Light={StaticResource Primary}, Dark={StaticResource DarkPrimary}}" />
                                    </DataTrigger>
                                </Border.Triggers>
                                <Border.GestureRecognizers>
                                    <TapGestureRecognizer Command="{Binding MostrarActivosCommand}" />
                                </Border.GestureRecognizers>
                                <Label Text="Activos"
                                       FontFamily="InterSemiBold"
                                       FontSize="11"
                                       TextColor="{AppThemeBinding Light={StaticResource Primary}, Dark={StaticResource DarkPrimary}}">
                                    <Label.Triggers>
                                        <DataTrigger TargetType="Label" Binding="{Binding FiltroEstado}" Value="Activos">
                                            <Setter Property="TextColor" Value="{StaticResource White}" />
                                        </DataTrigger>
                                    </Label.Triggers>
                                </Label>
                            </Border>

                            <Border Padding="12,6"
                                    BackgroundColor="{AppThemeBinding Light={StaticResource SurfaceColor}, Dark={StaticResource DarkSurfaceColor}}"
                                    Stroke="{AppThemeBinding Light={StaticResource BorderColor}, Dark={StaticResource DarkBorderColor}}"
                                    StrokeThickness="1"
                                    StrokeShape="RoundRectangle 14">
                                <Border.Triggers>
                                    <DataTrigger TargetType="Border" Binding="{Binding FiltroEstado}" Value="Inactivos">
                                        <Setter Property="BackgroundColor" Value="{AppThemeBinding Light={StaticResource Primary}, Dark={StaticResource DarkPrimary}}" />
                                        <Setter Property="Stroke" Value="{AppThemeBinding Light={StaticResource Primary}, Dark={StaticResource DarkPrimary}}" />
                                    </DataTrigger>
                                </Border.Triggers>
                                <Border.GestureRecognizers>
                                    <TapGestureRecognizer Command="{Binding MostrarInactivosCommand}" />
                                </Border.GestureRecognizers>
                                <Label Text="Inactivos"
                                       FontFamily="InterSemiBold"
                                       FontSize="11"
                                       TextColor="{AppThemeBinding Light={StaticResource Primary}, Dark={StaticResource DarkPrimary}}">
                                    <Label.Triggers>
                                        <DataTrigger TargetType="Label" Binding="{Binding FiltroEstado}" Value="Inactivos">
                                            <Setter Property="TextColor" Value="{StaticResource White}" />
                                        </DataTrigger>
                                    </Label.Triggers>
                                </Label>
                            </Border>
                        </HorizontalStackLayout>

                        <!-- Segunda fila: conserva el desplazamiento horizontal de catálogos -->
                        <ScrollView Orientation="Horizontal" HorizontalScrollBarVisibility="Never">
                            <HorizontalStackLayout BindableLayout.ItemsSource="{Binding Catalogos}" Spacing="8">
                                <BindableLayout.ItemTemplate>
                                    <DataTemplate x:DataType="model:CatalogoMaster">
                                        <Border Padding="12,6"
                                                BackgroundColor="{AppThemeBinding Light={StaticResource SurfaceColor}, Dark={StaticResource DarkSurfaceColor}}"
                                                Stroke="{AppThemeBinding Light={StaticResource BorderColor}, Dark={StaticResource DarkBorderColor}}"
                                                StrokeThickness="1"
                                                StrokeShape="RoundRectangle 14">
                                            <Border.Triggers>
                                                <DataTrigger TargetType="Border" Binding="{Binding IsSelected}" Value="True">
                                                    <Setter Property="BackgroundColor" Value="{AppThemeBinding Light={StaticResource Primary}, Dark={StaticResource DarkPrimary}}" />
                                                    <Setter Property="Stroke" Value="{AppThemeBinding Light={StaticResource Primary}, Dark={StaticResource DarkPrimary}}" />
                                                </DataTrigger>
                                            </Border.Triggers>
                                            <Border.GestureRecognizers>
                                                <TapGestureRecognizer
                                                    Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.SeleccionarCatalogoCommand}"
                                                    CommandParameter="{Binding .}" />
                                            </Border.GestureRecognizers>
                                            <Label Text="{Binding NombreMostrar}"
                                                   FontFamily="InterSemiBold"
                                                   FontSize="11"
                                                   TextColor="{AppThemeBinding Light={StaticResource Primary}, Dark={StaticResource DarkPrimary}}">
                                                <Label.Triggers>
                                                    <DataTrigger TargetType="Label" Binding="{Binding IsSelected}" Value="True">
                                                        <Setter Property="TextColor" Value="{StaticResource White}" />
                                                    </DataTrigger>
                                                </Label.Triggers>
                                            </Label>
                                        </Border>
                                    </DataTemplate>
                                </BindableLayout.ItemTemplate>
                            </HorizontalStackLayout>
                        </ScrollView>
                    </VerticalStackLayout>
                </Border>

                <CollectionView Grid.Row="4"
                                ItemsSource="{Binding Detalles}"
                                SelectionMode="None"
                                VerticalScrollBarVisibility="Never">
                    <CollectionView.EmptyView>
                        <VerticalStackLayout Padding="30" Spacing="6"
                                             HorizontalOptions="Center" VerticalOptions="Center">
                            <Label Text="Sin resultados" Style="{StaticResource CatalogHeadingText}" FontSize="16"
                                   TextColor="{AppThemeBinding Light={StaticResource TextMain}, Dark={StaticResource DarkTextMain}}" HorizontalTextAlignment="Center" />
                            <Label Text="No se encontraron registros para los filtros seleccionados."
                                   Style="{StaticResource CatalogPageSubtitle}" HorizontalTextAlignment="Center" />
                        </VerticalStackLayout>
                    </CollectionView.EmptyView>
                    <CollectionView.ItemTemplate>
                        <DataTemplate x:DataType="model:CatalogoDetalle">
                            <Border Margin="0,0,0,6" Padding="0" Style="{StaticResource CatalogCard}">
                                <Border.GestureRecognizers>
                                    <TapGestureRecognizer
                                        Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.AbrirDetalleCommand}"
                                        CommandParameter="{Binding .}" />
                                </Border.GestureRecognizers>
                                <Grid RowDefinitions="3,Auto">
                                    <BoxView Grid.Row="0" Color="{Binding ColorEstado}" />
                                    <Grid Grid.Row="1"
                                          Padding="12,7"
                                          RowDefinitions="Auto,Auto,Auto"
                                          ColumnDefinitions="*,Auto"
                                          RowSpacing="3">
                                        <VerticalStackLayout Grid.Row="0" Grid.Column="0" Spacing="1">
                                            <Label Text="CÓDIGO" Style="{StaticResource CatalogFieldLabel}" FontSize="9" />
                                            <Label Text="{Binding CodigoValor}" Style="{StaticResource CatalogFieldValueStrong}" FontSize="12" />
                                        </VerticalStackLayout>
                                        <Label Grid.Row="0" Grid.Column="1" Text="›" FontFamily="InterRegular" FontSize="24"
                                               TextColor="{AppThemeBinding Light={StaticResource TextLight}, Dark={StaticResource DarkTextLight}}" VerticalTextAlignment="Center" />
                                        <VerticalStackLayout Grid.Row="1" Grid.ColumnSpan="2" Spacing="1">
                                            <Label Text="VALOR / NOMBRE" Style="{StaticResource CatalogFieldLabel}" FontSize="9" />
                                            <Label Text="{Binding Valor}" Style="{StaticResource CatalogFieldValue}" FontSize="12"
                                                   MaxLines="1" LineBreakMode="TailTruncation" />
                                        </VerticalStackLayout>
                                        <VerticalStackLayout Grid.Row="2" Grid.ColumnSpan="2"
                                                             IsVisible="{Binding TieneDescripcion}" Spacing="1">
                                            <Label Text="DESCRIPCIÓN" Style="{StaticResource CatalogFieldLabel}" FontSize="9" />
                                            <Label Text="{Binding Descripcion}" FontFamily="InterRegular" FontSize="10"
                                                   TextColor="{AppThemeBinding Light={StaticResource TextMuted}, Dark={StaticResource DarkTextMuted}}"
                                                   MaxLines="1" LineBreakMode="TailTruncation" />
                                        </VerticalStackLayout>
                                    </Grid>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>

                <Grid Grid.RowSpan="5" IsVisible="{Binding IsBusy}" BackgroundColor="{AppThemeBinding Light={StaticResource WhiteTranslucent}, Dark={StaticResource DarkWhiteTranslucent}}">
                    <ActivityIndicator IsRunning="{Binding IsBusy}" Color="{AppThemeBinding Light={StaticResource Primary}, Dark={StaticResource DarkPrimary}}"
                                       HorizontalOptions="Center" VerticalOptions="Center" />
                </Grid>
            </Grid>
        </ContentView>
        <shared:BottomNavBar Grid.Row="1" />
    </Grid>
</ContentPage>
